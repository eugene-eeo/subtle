# Currently getSlot and setSlot use vm_call.
# We stress-test them below...

let getSlot = Object.rawGetSlot("rawGetSlot");

let x = {};
let record = {};

let custom_getSlot = Fn.new{ |k|
    return getSlot.callWithThis(this, k);
};

# x.k = 1
x.getSlot = custom_getSlot
x.k
